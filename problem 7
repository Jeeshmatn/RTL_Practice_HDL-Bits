	( Create a module that implements a NOR gate. 
             

                      module nor_gate(input a,b
                                                   output out);
                      assign out=!a *! b;
                      endmodule
